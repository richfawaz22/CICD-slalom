#this is used to close a pull request on main, dont need this now but just uncomment if you ever do 
#name: Branch 

#on:
  #pull_request_target:
    #types: [opened]

#jobs:
  #close:
    #runs-on: ubuntu-latest
    #steps:
    #- uses: superbrothers/close-pull-request@v3
      #with:
        #repo-token: ${{ secrets.GITHUB_TOKEN }}
        # Optional. Post a issue comment just before closing a pull request.
        #comment: "Thanks for this pull request"